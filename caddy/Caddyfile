# Caddyfile for Remnawave Bedolaga

{$ADMIN_DOMAIN} {
	encode gzip zstd
	# Serve built web-admin static files
	root * /srv/web-admin
	file_server

# Admin UI fetches API under /api prefix (strip /api before proxying)
handle_path /api/* {
	reverse_proxy remnawave_bot:8080
}
}
